stages:
- name: build
  steps:
  - runScriptConfig:
      image: node:latest
      shellScript: |-
        npm install --registry=https://registry.npm.taobao.org

        npm run build:prod
timeout: 60
notification: {}
