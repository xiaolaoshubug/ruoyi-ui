stages:
- name: build
  steps:
  - runScriptConfig:
      image: nodejs
      shellScript: npm install --registry=https://registry.npm.taobao.org
  - runScriptConfig:
      image: nodejs
      shellScript: npm run build:prod
timeout: 60
notification: {}
